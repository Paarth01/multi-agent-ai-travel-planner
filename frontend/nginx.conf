server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing means any path should serve
    # index.html and let React handle it, not 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
